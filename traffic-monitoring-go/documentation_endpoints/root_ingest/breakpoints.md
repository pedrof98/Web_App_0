# Debug Breakpoints Guide for POST /ingest Endpoint

## Critical Breakpoints (Set these in order of execution)

### 1. **Entry Point Breakpoint**
**File**: `app/handlers/ingestion.go`  
**Line**: `func (h *IngestionHandler) IngestEvent(c *gin.Context)`  
**Purpose**: See the incoming request and understand the full flow

### 2. **Request Body Reading**
**File**: `app/handlers/ingestion.go`  
**Line**: `body, err := io.ReadAll(c.Request.Body)`  
**Purpose**: Inspect the raw JSON payload being received

### 3. **Transaction Start**
**File**: `app/handlers/ingestion.go`  
**Line**: `err = h.DB.Transaction(func(tx *gorm.DB) error {`  
**Purpose**: Understand transaction-based processing

### 4. **Event Ingestion**
**File**: `app/siem/ingestion.go`  
**Line**: `func (e *EventIngester) IngestEvent(rawEventData []byte) error {`  
**Purpose**: See how raw JSON is parsed and normalized

### 5. **JSON Unmarshaling**
**File**: `app/siem/ingestion.go`  
**Line**: `if err := json.Unmarshal(rawEventData, &rawEvent); err != nil {`  
**Purpose**: Inspect the parsed RawEvent structure

### 6. **Log Source Creation/Lookup**
**File**: `app/siem/ingestion.go`  
**Line**: `err := e.DB.Raw("SELECT id FROM log_sources WHERE name = ? LIMIT 1", rawEvent.SourceName).Scan(&logSourceID).Error`  
**Purpose**: See how log sources are managed

### 7. **Security Event Creation**
**File**: `app/siem/ingestion.go`  
**Line**: `securityEvent := models.SecurityEvent{`  
**Purpose**: See how raw events become security events

### 8. **Rule Engine Evaluation**
**File**: `app/handlers/ingestion.go`  
**Line**: `if err := ruleEngine.EvaluateEvent(&securityEvent); err != nil {`  
**Purpose**: Watch rule evaluation process

### 9. **Rule Engine Processing**
**File**: `app/siem/rule_engine_enhanced.go`  
**Line**: `func (re *EnhancedRuleEngine) EvaluateEvent(event *models.SecurityEvent) error {`  
**Purpose**: Deep dive into rule evaluation logic

### 10. **Elasticsearch Indexing**
**File**: `app/handlers/ingestion.go`  
**Line**: `go func(event *models.SecurityEvent, alertList []models.Alert) {`  
**Purpose**: See asynchronous Elasticsearch processing

## Inspection Variables

At each breakpoint, inspect these key variables:

- `body` - Raw request payload
- `rawEvent` - Parsed event structure  
- `logSourceID` - Database ID of the log source
- `securityEvent` - Final security event object
- `alerts` - Any alerts generated by rules
- `err` - Error conditions at each step